version: 2
  
jobs:
  - docker:
      image: golang:1.18-buster
      copyGitHistory: true
    template:
      name: go
      # We run the tests as part of every merge
      skip:
        - test

branches:
  - name: main
