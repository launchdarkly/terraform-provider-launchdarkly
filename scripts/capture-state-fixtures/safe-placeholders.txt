# Safe placeholder values that fixture captures may contain.
#
# Lines starting with # and blank lines are ignored.
# Entries are matched verbatim against substrings caught by scan.sh.
# Keep this list narrow — the scan is intended to catch real secrets;
# every entry here weakens that guarantee.

# Synthetic project / environment identifiers used by capture.sh:
fixture-project-1
fixture-project-2
fixture-env-test
fixture-env-prod

# Tokens / keys: only synthetic placeholders, never real values.
fixture-token-PLACEHOLDER
sdk-00000000000000000000000000000000
mob-00000000000000000000000000000000

# Zero-UUIDs sometimes show up in deterministic captures of new resources.
00000000-0000-0000-0000-000000000000

# Sanitiser-produced placeholders (sanitize.jq). Non-UUID shape so GitHub
# secret scanning doesn't flag fixtures and scan.sh's `api-<uuid>` /
# `rel-<uuid>` patterns don't fire either.
FIXTURE_REDACTED_TOKEN
FIXTURE_REDACTED_RELAY_KEY

# Static fixture-style UUIDs from the capture-script seed.
fixture-uuid-aaaaaaaa-aaaa-aaaa-aaaa-aaaaaaaaaaaa
